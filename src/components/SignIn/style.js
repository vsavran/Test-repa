import {makeStyles} from "@material-ui/core/styles";

export const signInStyles = makeStyles((theme) => ({
	root: {
		backgroundColor: theme.palette.secondary.btn,
		color:theme.palette.primary.white
	},

}));
